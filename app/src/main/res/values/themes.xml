<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PETSTORE" parent="Theme.MaterialComponents.DayNight.Bridge">
        <item name="colorPrimary">@color/whiteBlue</item>
<!--        <item name="android:colorPrimaryDark">@color/grey</item>-->
<!--        <item name="android:statusBarColor">@color/white</item>-->
        <item name="android:navigationBarColor">@color/white</item> <!-- 设置导航栏颜色 -->
    </style>

    <style name="Theme.PETSTORE" parent="Base.Theme.PETSTORE" />
    <style name="TextAppearance.CalendarDayHeader" parent="TextAppearance.AppCompat.Medium">
        <item name="android:textSize">18px</item> <!-- 设置字体大小 -->
        <item name="android:textColor">@color/white</item> <!-- 设置字体颜色 -->
    </style>
    <style name="Widget.CalendarView.Custom" parent="Theme.AppCompat">
        <!--周字体颜色-->
        <item name="android:weekDayTextAppearance">@style/TextAppearance.CalendarDayHeader</item>
        <!--日字体颜色-->
        <item name="android:dateTextAppearance">@color/white</item>
        <!--选中后背景-->
        <item name="colorAccent">@color/whiteRed</item>
        <item name="colorPrimary">@color/white</item>
        <!--选中后字体颜色-->
        <item name="android:textColorPrimaryInverse">@color/white</item>
        <!--标题颜色-->
        <item name="android:textColorPrimary">@color/white</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_log</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>


</resources>